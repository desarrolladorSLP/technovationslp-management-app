import { FilteruseractivePipe } from './filteruseractive.pipe';

describe('FilteruseractivePipe', () => {
  it('create an instance', () => {
    const pipe = new FilteruseractivePipe();
    expect(pipe).toBeTruthy();
  });
});
