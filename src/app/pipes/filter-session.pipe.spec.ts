import { FilterSessionPipe } from './filter-session.pipe';

describe('FilterSessionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSessionPipe();
    expect(pipe).toBeTruthy();
  });
});
